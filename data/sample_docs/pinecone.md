# Pinecone

Pinecone is a managed cloud vector database. It provides serverless indexes, namespaces, metadata filters, and an API-key based workflow for applications that do not want to operate their own vector search infrastructure.

Pinecone is a good fit for cloud prototypes, managed production operations, and teams that prefer provider-managed scaling.
