# Qdrant

Qdrant is a vector database that can run locally with Docker Compose. It stores vectors in collections and supports payload filters for metadata such as source, document id, or chunk index.

Qdrant uses HNSW indexing for nearest-neighbor retrieval and is a good fit when a RAG team wants local development, infrastructure control, and a path to self-hosted production.
